# Non-secret Cloud Functions parameters for parkqueen-46475363-ccf36.
# Read by the Functions emulator and by `firebase deploy` for this project.
# NEVER put secrets here — secrets live in Secret Manager (defineSecret).
#
# Marketing waitlist (see docs/WAITLIST.md). Transitional cutover state:
# Turnstile accepts both hosts, but confirmation links still use staging.
# Switch the base URL to https://parqueen.app in a separate reviewed commit.
WAITLIST_CONFIRM_BASE_URL=https://parqueen-marketing.web.app
WAITLIST_ALLOWED_HOSTNAMES=parqueen-marketing.web.app,parqueen.app
CURB_RESOLVER_MODE=shadow
CURB_RESOLVER_URL=https://parqueen-curb-resolver-spike-oxbozdhlwa-uc.a.run.app
# Default is already 0 at runtime. The emulator still interactively prompts for
# any defineString that is missing from this file (firebase-tools treats the
# SDK default as a prompt suggestion, not a skip), which hangs CI after
# "Serving at port". Keep this explicit so discovery can finish.
CURB_SHADOW_SAMPLE_PERMILLE=0
